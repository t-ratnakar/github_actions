on:
    push:
        branches:
            - main
    label:
        types:
            - created
    issues:
        types: [opened, labeled]
jobs:
    print_hello:
        runs-on: ubuntu-latest
        steps:
            - name: Print Hello World
              run: echo "Hello, World!"